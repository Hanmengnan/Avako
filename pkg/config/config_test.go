package config

import (
	"log"
	"testing"
)

// import "testing"

// func TestLoadConfig(t *testing.T) {
// 	configFile := "../config/config.json"
// 	LoadConfig(configFile)
// }

func TestLoadConfig(t *testing.T) {
	log.Print("")
}
